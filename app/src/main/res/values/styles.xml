<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Core -->
    <style name="Theme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/primary_black_text</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">@color/systemBarColor</item>
        <item name="android:navigationBarColor">@color/systemBarColor</item>
    </style>

    <style name="Theme.App" parent="Theme" />

    <style name="Theme.App.Launch" parent="Theme.App">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
